<Ui xmlns="http://www.blizzard.com/wow/ui/"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.blizzard.com/wow/ui/ ../../FrameXML/UI.xsd">

	<Button name="ClickCastingToggleButton" inherits="TabSystemButtonArtTemplate" text="Click Binding" motionScriptsWhileDisabled="true" hidden="true">
		<Size x="120" y="32" />
		<Scripts>
			<OnLoad>
				self:SetTabSelected(false)
				self:HandleRotation()
			</OnLoad>
			<OnClick>
				ToggleClickBindingFrame()
			</OnClick>
			<OnEnter>
				GameTooltip:SetOwner(self, 'ANCHOR_BOTTOM');
				GameTooltip:SetText("Click Cast Bindings");
				if self:IsEnabled() then
					GameTooltip:AddLine('Click to toggle the Click Cast Bindings mode');
				else
					GameTooltip:AddLine('Click Cast Bindings are currently active');
				end
				GameTooltip:Show();
			</OnEnter>
			<OnLeave>GameTooltip:Hide()</OnLeave>
		</Scripts>
	</Button>
</Ui>
